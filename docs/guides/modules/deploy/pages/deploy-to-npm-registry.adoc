= Publish to npm
:page-platform: Cloud
:page-description: Publish packages to npm from CircleCI using OIDC trusted publishing, the recommended tokenless approach.
:experimental:

In this how-to guide, you will learn how to configure CircleCI to publish to npm using trusted publishing. Trusted publishing uses OpenID Connect (OIDC) to exchange a short-lived CircleCI token for an npm publish token at publish time, so there is no long-lived `NPM_TOKEN` to store, rotate, or leak.

For a functional example you can clone and adapt, see the https://github.com/CircleCI-Public/sign-and-publish-examples/tree/main/npm[CircleCI npm trusted publishing example].

TIP: If trusted publishing is not an option for your package or registry, see xref:deploy:deploy-to-npm-registry-with-token.adoc[Publish to npm with an authentication token].

[#introduction]
== Introduction

Trusted publishing is the recommended way to publish to npm from CircleCI. Compared to using a long-lived `NPM_TOKEN` environment variable, trusted publishing:

* Removes the need to create, store, and rotate a publish token.
* Issues a short-lived publish token that is valid only for the duration of the publishing job.
* Lets you scope publishing to a specific organization, project, pipeline definition, and (optionally) context.

NOTE: Provenance attestations are not currently supported for packages published from CircleCI via trusted publishing.

[#prerequisites]
== Prerequisites

* A CircleCI Cloud account integrated with a supported VCS. See xref:getting-started:first-steps.adoc[Sign up and Try CircleCI] for more information.
* A project on CircleCI that builds your npm package.
* An npm package that has already been published at least once. npm requires an existing package before a trusted publisher can be configured for it.
* npm CLI version 11.5.1 or later, and Node.js 22.14.0 or later, in the Docker image (or executor) used by your publishing job.
* Maintainer or owner access to the npm package.

[#gather-the-data-you-need]
== 1. Gather the data you need

npm needs four CircleCI identifiers to configure your project as a trusted publisher. Collect these before you visit npm.

* *Organization ID* (required): Your CircleCI organization ID, in UUID format. From the CircleCI web app, go to menu:Org Settings[Overview].
+
.Organization ID on the Org Settings Overview page
image::guides:ROOT:npm-trusted-publishing/organization-id.png[CircleCI Org Settings Overview page showing the Organization ID field]

* *Project ID* (required): Your CircleCI project ID, in UUID format. Go to menu:Project Settings[Overview].
+
.Project ID on the Project Settings Overview page
image::guides:ROOT:npm-trusted-publishing/project-id.png[CircleCI Project Settings Overview page showing the Project ID field]

* *Pipeline Definition ID* (required): The pipeline definition ID, in UUID format. Go to menu:Project Settings[Project Setup]. There you will see your list of pipelines. Find the pipeline that will run the publishing workflow and copy its ID.
+
.Pipeline Definition ID on the Project Setup page
image::guides:ROOT:npm-trusted-publishing/project-def-id.png[CircleCI Project Settings Project Setup page showing the list of pipelines with their definition IDs]

* *VCS origin* (required): The version control system origin URL for your project, for example `github.com/myorg/myrepo` or `gitlab.com/mylab/myproject`.

* *Context IDs* (optional): If you intend to restrict publishing to a CircleCI context (recommended, see <<lock-it-down,Lock it down with protected contexts>>), find the UUID for each context under menu:Org Settings[Contexts] and selecting the context.
+
.Context ID on the Contexts detail page
image::guides:ROOT:npm-trusted-publishing/context-id.png[CircleCI Org Settings Contexts detail page showing the Context ID]

[#set-up-the-trusted-publisher-on-npm]
== 2. Set up the trusted publisher on npmjs.com

. Sign in to https://www.npmjs.com[npmjs.com] and navigate to your package page.

. Select the *Settings* tab.

. Under *Trusted Publishing*, select *CircleCI*.
+
.Selecting CircleCI as the trusted publisher on npmjs.com
image::guides:ROOT:npm-trusted-publishing/npm-settings.png[npm package Settings tab showing the Trusted Publisher section with CircleCI selected as the provider]

. Fill in the fields with the values you collected in the previous step:
+
** Organization ID
** Project ID
** Pipeline Definition ID
** VCS origin
** Context IDs (optional)
+
.Trusted publisher form with the CircleCI identifiers filled in
image::guides:ROOT:npm-trusted-publishing/npm-trusted-form.png[npm Trusted Publisher form for the CircleCI provider with Organization ID, Project ID, Pipeline Definition ID, Context IDs, and VCS origin populated]

. Choose which actions this trusted publisher can perform (`npm publish`, `npm stage publish`, or both) and save.

[#configure-your-circleci-pipeline]
== 3. Configure your CircleCI pipeline

Add a publishing job to your `.circleci/config.yml`. The job retrieves an OIDC token for the `npm:registry.npmjs.org` audience and assigns it to the `NPM_ID_TOKEN` environment variable. When `NPM_ID_TOKEN` is set, the npm CLI automatically exchanges it for a short-lived publish token, so no `NPM_TOKEN` is needed.

The job is attached to a CircleCI xref:security:contexts.adoc[context] (here named `trusted-publishing-guard`) and the workflow filters to the `main` branch only. The context is the lockdown point — see <<lock-it-down,Lock it down with protected contexts>> for how to restrict it.

```yaml
version: 2.1

jobs:
  publish:
    docker:
      - image: cimg/node:22.14
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm ci
      - run:
          name: Run tests
          command: npm test
      - run:
          name: Build
          command: npm run build --if-present
      - run:
          name: Publish to npm
          command: |
            export NPM_ID_TOKEN=$(circleci run oidc get --claims '{"aud": "npm:registry.npmjs.org"}')
            npm publish --access public

workflows:
  publish:
    jobs:
      - publish:
          context:
            - trusted-publishing-guard
          filters:
            branches:
              only:
                - main
```

For background on OIDC tokens in CircleCI, see xref:permissions-authentication:openid-connect-tokens.adoc[Use OpenID Connect tokens in jobs].

[#lock-it-down]
== 4. Lock it down with protected contexts

You can further restrict who and what can run the publishing job by attaching the job to a xref:security:contexts.adoc[CircleCI context] and applying restrictions to that context. If you do this, remember to record the context's UUID under *Context IDs* when you configure the trusted publisher on npm (see <<set-up-the-trusted-publisher-on-npm,Step 2>>).

[#group-restrictions]
=== Security group restrictions

xref:security:contexts.adoc#security-group-restrictions[Security group restrictions] limit job execution to members of a specific group. Anyone outside the group who attempts to run the job fails. This is useful when you want only a small set of people to be able to publish.

[#project-restrictions]
=== Project restrictions

xref:security:contexts.adoc#project-restrictions[Project restrictions] limit a context to one or more specific projects. With a project restriction in place, a job in any other project that tries to use the context will fail. This is useful for ensuring the publishing context can only be used by the project that owns your npm package.

[#expression-restrictions]
=== Expression restrictions

xref:security:contexts.adoc#expression-restrictions[Expression restrictions] are more powerful. The most common use is restricting a context to a specific branch. For example:

```
pipeline.git.branch == "main"
```

This causes any job on a different branch that tries to use the context to fail.

[#ssh]
=== SSH

npm rejects any OIDC token whose `ssh_rerun` claim is `true`. As defense in depth, you can also add the following expression restriction to your publishing context so that the job cannot be re-run with SSH at all:

```
not job.ssh.enabled
```

You can combine expressions to restrict the context to a specific branch and disallow SSH reruns in a single rule:

```
pipeline.git.branch == "main" and not job.ssh.enabled
```

.Example publishing context with project and expression restrictions applied
image::guides:ROOT:npm-trusted-publishing/context-restrictions-examples.png[CircleCI context settings showing a project restriction limiting the context to one project and an expression restriction combining a branch match with not job.ssh.enabled]

[#publish-a-new-version]
== 5. Publish a new version

With the workflow above, every merge to `main` publishes a new version. To bump the package version, run `npm version` on a feature branch:

```shell
npm version 10.0.1
```

Open a pull request and merge to `main`. CircleCI runs the publishing workflow on the merge commit, and the npm CLI exchanges the OIDC token for a short-lived publish token to upload the package.

TIP: As an alternative to manually bumping the version, you can compute a version at build time from `CIRCLE_BUILD_NUM` so each merge to `main` produces a monotonically increasing version automatically. See the https://github.com/CircleCI-Public/sign-and-publish-examples/tree/main/npm[CircleCI npm trusted publishing example] for one approach.

[#self-hosted-runners]
== Self-hosted runners

Trusted publishing from CircleCI self-hosted runners works.

[#notes]
== Notes

* npm supports https://docs.npmjs.com/staged-publishing[staged publishing]. You can configure your trusted publisher to allow `npm stage publish` in addition to (or instead of) `npm publish`, which lets you stage a release for review before promoting it.
* Pull requests from forks do not have access to CircleCI secrets, including OIDC tokens. A fork cannot trigger a publish.

[#track-deployments-with-deploy-markers]
== Track your deployments with deploy markers

Deploy markers provide a way to track and manage your npm package publications in the CircleCI web app. When you add deploy markers to your deployment job, you can view a timeline of all deployments, track their status, and enable rollback and deploy pipelines.

You have two options for setting up deploy markers:

* *In-app setup*: Use the guided setup in the CircleCI web app when configuring a xref:set-up-rollbacks.adoc[Rollback Pipeline] or xref:set-up-deploys.adoc[Deploy Pipeline]. The setup will walk you through adding deploy markers to your configuration. If you are using GitHub and have the CircleCI GitHub App installed, you can use AI to generate the deploy marker configuration automatically.

* *Manual setup*: Add deploy marker commands directly to your `.circleci/config.yml` file by following the xref:configure-deploy-markers.adoc[Configure Deploy Markers] guide.

Both approaches will enable you to track deployment history and manage rollbacks directly from the CircleCI web app.

[#questions-and-feedback]
== Questions? Feedback?

If you have questions or feedback, drop by the https://discuss.circleci.com/[CircleCI Discuss forum] or the https://discord.gg/circleci[CircleCI Discord community].
