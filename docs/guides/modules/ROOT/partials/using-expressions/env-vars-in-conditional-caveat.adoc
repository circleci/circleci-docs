* Environment variables cannot be used in expressions and logic statements. You can only use pipeline values or pipeline parameters.
+
Expressions and logic statements are evaluated at compilation time whereas the actual values of environment variables only become available at run time.