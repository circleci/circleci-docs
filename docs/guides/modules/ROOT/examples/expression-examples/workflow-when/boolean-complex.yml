workflows:
  my-workflow:
# This workflow runs when: branch is "main" AND param1 is false AND (param2 OR param3 is true)
    when: pipeline.git.branch == "main" and not pipeline.parameters.param1 and (pipeline.parameters.param2 or pipeline.parameters.param3)
    jobs:
      - build