= Pause or stop pipelines during an incident
:page-platform: Cloud, Server v4+
:page-description: Decide when to cancel running work, block new pipelines, stop building, coordinate followers, or rotate and remove secrets during an incident.
:experimental:

During an incident you may need to **stop pipelines that are already running** and **prevent additional pipelines from starting**. This page gives a practical order of operations for common scenarios:

* **Compromised secret or credential.** Prevent new pipelines with **Block all new work**, cancel running work, and rotate the affected secrets before any more jobs can use them.
* **Suspicious or unauthorized code.** Stop pipelines that are executing untrusted code, then investigate what ran.
* **Suspicious dependency, orb, or image.** Halt work that could continue to use a compromised third-party component, then investigate where it ran.
* **Compromised account or insider threat.** Halt all work, review audit logs, and rotate credentials the account had access to.
* **Operational incident or release issue.** Pause new work while your team verifies configuration, dependencies, or external systems.

CircleCI provides several controls and they work differently. The sections below explain each option and when to use it.

After you have paused or stopped pipelines, see xref:orchestrate:analyze-pipelines-during-an-incident.adoc[Analyze Pipelines and Job Steps During an Incident] to investigate what ran and what the incident exposed.

NOTE: For server customers, **Block all new work** is available in Server 4.10+. See xref:orchestrate:skip-build.adoc#block-all-new-work[Block All New Work] for details.

NOTE: Before you begin, confirm which controls your org supports based on integration type (and Server version if applicable). If **Block all new work** is unavailable, cancel running workflows from the xref:orchestrate:pipelines.adoc#pipelines-dashboard[Pipelines Dashboard] and use **Stop Building** where supported.

[#recommended-order]
== Recommended order of operations

. **Prevent new pipelines first** with **Block all new work** at **organization** or **project** scope. This is a single toggle and is the fastest way to stop the bleeding: CircleCI rejects incoming pipelines and users cannot rerun workflows. Your VCS integration stays in place. See xref:orchestrate:skip-build.adoc#block-all-new-work[Block All New Work].
+
.Block all new work (Advanced project settings)
image::guides:ROOT:incident-block-all-new-work-project-setting.png[Advanced project settings showing the Block all new work from starting for this project toggle]
. **Cancel running work and disconnect the project** with **Stop Building** (GitHub OAuth and Bitbucket Cloud). Stop Building removes all project followers, **cancels all running workflows asynchronously**, and removes the webhook and deploy key from the repository. Cancellation happens in the background. The UI responds before all workflows have finished stopping. Use **Set Up Project** to reconnect later. See xref:security:stop-building-a-project-on-circleci.adoc[Stop Building a Project on CircleCI]. This option is **not** available for all integration types; see the same page for support and limitations.
+
.Stop Building (Project overview)
image::guides:ROOT:incident-stop-building-project-setting.png[Project overview showing the Stop Building section]
+
NOTE: **Block all new work** does **not** cancel workflows that are already running. It only prevents incoming pipelines and reruns. If you need to stop in-flight work and your integration supports it, **Stop Building** is the action that cancels running workflows. For integrations without Stop Building (GitHub App, GitLab, Bitbucket Data Center), cancel workflows individually from the xref:orchestrate:pipelines.adoc#pipelines-dashboard[Pipelines Dashboard].
. **Coordinate who follows the project.** The **Projects** list controls whether a user follows a project for their dashboard. For some integrations, **following** a project can restore GitHub-side resources (such as webhook and deploy key) after a stop-build flow, which can look like "nothing happened" if another teammate follows the project during an incident. Align with your team so that stopping or blocking work is not unintentionally undone. See xref:about-circleci:introduction-to-the-circleci-web-app.adoc#projects[Projects in the Web App].
. **Rotate or remove secrets** the incident may have exposed, or generate an inventory for your security team. The API and CLI return **masked** values for project variables by design; you cannot bulk-export plaintext secrets to compare them. Use the CLI or API to **list names**, **remove** bad variables, and **store** replacements. See <<#secrets-and-credentials,Secrets and credentials>>.

[#block-vs-stop-building]
== Block all new work vs Stop Building

The table below compares the two controls.

[cols="1,2,2",options="header"]
|===
| | **Block all new work** | **Stop Building**

| **What it does**
| Stops **new** pipelines and reruns at CircleCI. VCS integration remains.
| Removes all project followers, **cancels all running workflows**, and removes the project's VCS connection (webhook and deploy key). You use **Set Up Project** to connect again later.

| **Running workflows**
| Already-running workflows **continue** until they finish, fail, or you cancel them. **Block all new work** does **not** cancel in-flight work.
| **Cancels all running workflows** asynchronously (in the background). The UI responds before all cancellations complete.

| **Typical use**
| Fast circuit breaker during an incident while you keep the project connected. Use when you want to stop new work but let in-progress builds finish.
| Full project shutdown: stops new work, cancels running work, and disconnects the VCS integration. Use when you need everything to stop.

| **Where**
| **Org** or **Project Settings** > **Advanced**
| **Project Settings** > **Overview** (bottom of page)

| **Details**
| xref:orchestrate:skip-build.adoc#enable-block-new-work-for-a-project[Skip CI, Auto-Cancel, and Block New Pipelines]
| xref:security:stop-building-a-project-on-circleci.adoc[Stop Building a Project on CircleCI]
|===

[#secrets-and-credentials]
== Secrets and credentials

After you have blocked new work or stopped building, rotate or remove secrets that the incident may have exposed. This includes context variables, project variables, API tokens, SSH keys, and runner tokens. The public API and CLI return **masked** values by design. Inventory secret **names** and **last-updated** metadata first, then replace or remove the affected values.

[#triage]
=== Triage and audit logs

Start here if you suspect a leak:

* link:https://support.circleci.com/hc/en-us/articles/31307485547419[What to do if you suspect you have a secret leaked from CircleCI]
* xref:security:audit-logs.adoc[Audit Logs]

To inventory variable names across multiple projects, use the open source link:https://github.com/CircleCI-Public/CircleCI-Env-Inspector[CircleCI Environment Inspector] (run with a xref:toolkit:managing-api-tokens.adoc[Personal API Token]; treat output as sensitive).

[#environment-variables]
=== Environment variables

Environment variables are stored in **contexts** (shared across projects) and **project settings** (scoped to one project). Both store secrets as masked values. You rotate them differently depending on where they live and how your project connects to CircleCI.

**Context variables.** Use the CLI or API:

* xref:security:contexts.adoc#rotating-environment-variables[Rotate Environment Variables With the CLI or API]
* xref:security:contexts.adoc#deleting-environment-variables[Remove an Environment Variable From a Context]

**Project variables.** The method depends on your integration type:

* **GitHub App / GitLab projects:** Use `circleci project secret` commands. See xref:toolkit:how-to-use-the-circleci-local-cli.adoc#project-management[Project Management] in the CLI guide.
* **GitHub OAuth / Bitbucket Cloud projects:** Use the link:https://circleci.com/docs/api/v2/#tag/Project[Project API].
* **Web app (all integration types):** See xref:security:set-environment-variable.adoc[Set an Environment Variable].

[#ssh-keys-and-runners]
=== SSH keys and self-hosted runners

* **Project SSH keys:** xref:security:rotate-project-ssh-keys.adoc[Rotate Project SSH Keys].
* **Runner tokens:** See the runner token sections in xref:toolkit:how-to-use-the-circleci-local-cli.adoc[How to Use the CircleCI Local CLI].

[#github-oauth-webhooks]
=== GitHub OAuth webhooks and deploy keys

* **Rotate the GitHub webhook secret** (GitHub OAuth project triggers). See link:https://support.circleci.com/hc/en-us/articles/49131607657243[Rotating the GitHub webhook secret for CircleCI GitHub OAuth project triggers] for the documented flow with CircleCI-specific notes.
* **Disconnect without the CircleCI app** (for example the UI is unavailable). Manually remove the CircleCI webhook and deploy key from the repository: link:https://support.circleci.com/hc/en-us/articles/360021666393[How to stop building by manually removing the CircleCI webhook and deploy key from your GitHub repository]. This stops delivery of events; it is not a substitute for rotating the webhook secret if that credential leaked.
* **Use Stop Building in the app** when you can: xref:security:stop-building-a-project-on-circleci.adoc[Stop Building a Project on CircleCI] removes the webhook and deploy key for supported integrations and cancels running workflows.

After a security incident, **regenerate** deploy keys and webhooks by reconnecting the project in CircleCI (or repeating the GitHub webhook setup) to invalidate old endpoints and secrets.

[#api-tokens-and-user-keys]
=== API tokens and user keys

* **Personal API tokens:** Regenerate from **User Settings > Personal API Tokens** in the web app. See xref:toolkit:managing-api-tokens.adoc[Managing API Tokens].
* **Project API tokens:** Regenerate from **Project Settings > API Permissions**. These are deprecated but still in use on some projects.
* **User keys (checkout keys):** Manage from **Project Settings > SSH Keys**. Remove compromised keys and add replacements.

[#other-options]
== Other options (depending on your situation)

* **Skip the next push only:** Add `[ci skip]` or `[skip ci]` to the latest commit (see xref:orchestrate:skip-build.adoc#skip-jobs[Skip CI for a Specific Push]). This does not stop work that is already queued or running across the org.
* **Feature availability:** Triggers, **Stop Building**, and **Block all new work** vary by how your org connects to your VCS. See the xref:integration:version-control-system-integration-overview.adoc[VCS Integration Overview] for what applies to GitHub OAuth, GitHub App, GitLab, and Bitbucket.
* **Scale Plan (optional policy-based pause):** **Block all new work** at **organization** scope is enough to stop pipelines org-wide. On **Scale**, you can also use **config policy** to fail pipelines with a **custom message** and **exempt** specific projects (for example a repo used only to publish policies). See xref:config-policies:config-policy-management-overview.adoc[Policy Management Overview] and the Help Center sample link:https://support.circleci.com/hc/en-us/articles/42043259044379[Blocking builds org-wide through config policies].
* **Bulk "unfollow" or scripted stop (GitHub OAuth-style projects):** Some teams use a scripted loop against the link:https://circleci.com/docs/api/v1/[API v1] to unfollow multiple repositories at once. That pattern operates **per user** who runs it and can require **Set Up Project** again later. Read link:https://support.circleci.com/hc/en-us/articles/360019776413[How to Mass Unfollow Projects From CircleCI] before using it in an emergency.

[#see-also]
== See also

* xref:orchestrate:analyze-pipelines-during-an-incident.adoc[Analyze Pipelines and Job Steps During an Incident]
* xref:orchestrate:skip-build.adoc[Skip CI, Auto-Cancel, and Block New Pipelines]
* xref:security:stop-building-a-project-on-circleci.adoc[Stop Building a Project on CircleCI]
* xref:orchestrate:pipelines.adoc[Pipelines Overview and Setup]
* xref:config-policies:config-policy-management-overview.adoc[Policy Management Overview]
* xref:security:contexts.adoc[Using Contexts]
* xref:security:env-vars.adoc[Introduction to Environment Variables]
* xref:security:audit-logs.adoc[Audit Logs]
* xref:security:set-environment-variable.adoc[Set an Environment Variable]
* xref:toolkit:managing-api-tokens.adoc[Managing API Tokens]
* xref:toolkit:how-to-use-the-circleci-local-cli.adoc[How to Use the CircleCI Local CLI]
* xref:toolkit:local-cli.adoc[Install and Configure the CircleCI Local CLI]
