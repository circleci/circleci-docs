version: 2
jobs:
  build:
    machine: true
    steps:
      - run:
          name: ALGOLIA Key
          command: |
            echo ${CIRCLE_BRANCH}
            echo ${ALGOLIA_PRIVATE_KEY}
            echo ${ALGOLIA_API_KEY} >> key.txt
            cat key.txt
